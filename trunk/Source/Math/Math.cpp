#include "Includes.h"
#include "Import.h"

namespace P3D
{
    const Vector Vector::Zero = Vector(0, 0, 0);
    const Matrix Matrix::Identity = Matrix(1, 0, 0,
                                           0, 1, 0,
                                           0, 0, 1);    
}